# 提示

1. 注意坐标计算！
2. 注意坐标计算！
3. 注意坐标计算！
4. 如果找不到错误在哪里建议先减小矩阵规模、S、T、U大小（保持T=S*U），找到一个尽可能小的错误案例，之后把A、B、C矩阵的内容及计算过程中内核读取的元素坐标及值print出来，像这样`printf("Asub[%u][%u]([%u][%u]):%9.6f\n",ay,ax,row+ay,kk+ax,Asub[ay][ax]);`
5. 如果没有加速效果有可能你的代码里有重复计算，也可能实验平台有问题，可以改变T、U、S大小查看是否出现加速效果
6. 助教的参考实现是将B矩阵放入寄存器，也即A矩阵放入shared memory分块是`Asub[U][S]`。输出是`N:  4096  time1: 102.077064514  time2: 67.535476685 time3: 29.737787247`

# 报告问题（可能更新，以这里为准）

1. 请分析三种GPU矩阵乘法中对GPU global memory、shared memory、register的访问次数（忽略非矩阵元素的寄存器访问，如坐标索引变量row对寄存器的访问）。假定block_size大小为B，矩阵大小为N，T、U、S也均用字母表示  
2. 寄存器矩阵乘法减少了线程数目及shared memory使用量，但增加了寄存器使用量。请结合课程所学分析这样做有何好处，有何坏处。（提示：从warp个数及block 所需资源及对GPU调度影响上分析）  
3. 请结合课程所学从线程访存合并方面分析寄存器矩阵乘法对global memory的访问哪里可以继续优化。这一优化手段对基础矩阵乘法、分块矩阵乘法有效吗？请说明理由并给出优化位置。  
